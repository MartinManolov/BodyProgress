$(document).ready(function () {
    $('#search').autocomplete({
        source: '/api/Search/search',
        select: function (event, ui) {
            selectedUsername = ui.item.label;

            window.location.href= "/Profiles/Info?username=" + selectedUsername;
                
           
        }
        
    });
});